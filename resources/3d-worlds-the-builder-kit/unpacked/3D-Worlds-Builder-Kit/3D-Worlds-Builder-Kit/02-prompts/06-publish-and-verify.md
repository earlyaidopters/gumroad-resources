# Host the finished build

New reusable prompt. Replace bracketed values and attach the referenced files.

---

Prepare the approved production build for the host I selected: [HOST]. Confirm the intended audience, account ownership, required runtime, build output directory and asset paths. Identify anything that cannot work as a static site. Keep API keys and account credentials outside the public bundle.

Create a reproducible archive and record the source revision. Publish only within my current authorization. If setup or a consequential account action needs me, prepare everything else first and explain the precise remaining step. After deployment, open the actual hosted URL, enter the game, complete a lesson, reload, check assets and downloads, and verify the intended access policy. Record deployment identity, verification evidence and a rollback method. Do not equate a successful upload with a working game.

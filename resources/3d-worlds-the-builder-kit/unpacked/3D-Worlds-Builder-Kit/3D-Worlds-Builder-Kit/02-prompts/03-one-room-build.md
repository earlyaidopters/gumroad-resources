# Build the first finished lesson

New reusable prompt. Replace bracketed values and attach the referenced files.

---

Implement the approved first lesson end to end. Produce the actual browser experience: arrival, character, camera, interaction, correct and incorrect feedback, hint, retry and completion. Use placeholder geometry only while proving interaction, then replace the visible focal assets to the approved quality bar.

Keep lesson logic separate from the renderer and content. Include a usable fallback if the 3D scene cannot load. Run the application and test through visible controls. Capture the start, interaction and completion states. Report what you directly observed, which tests passed and what remains unverified. Save an exact runnable checkpoint. Stop expansion until this representative lesson establishes the visual and teaching standard.
